# תמונות פרופיל קטנות

יש להריץ את `supabase/migrations/055_profile_avatars.sql` ב־Supabase SQL Editor ולפרוס את גרסת האפליקציה המעודכנת. המיגרציה יוצרת את דלי האחסון `profile-avatars`, הרשאות כתיבה לתמונה האישית בלבד ופונקציה לעדכון הפרופיל. אפשר להריץ אותה שוב בבטחה.

## שימוש

בפרופיל: בחירת תמונה → תצוגה מקדימה → שמירת התמונה. ניתן לבטל לפני השמירה, להחליף תמונה או להסיר אותה. השם נשאר לצד התמונה. אורחים, משתמשים ללא תמונה ותמונות שלא נטענו מוצגים עם אות כגיבוי.

## מגבלות וחיסכון

- קובץ מקור: JPG, PNG או WebP עד 10MiB. קבצים אחרים, כולל HEIC, דורשים המרה לפני הבחירה.
- חיתוך מרכזי לריבוע ודחיסה בדפדפן: עד 256×256, ללא הגדלה של תמונות קטנות. אם צריך, הרזולוציה יורדת ל־192 או 128.
- יעד של עד 20KiB, ותקרה קשיחה של 50KiB לקובץ שנשלח. נשמר WebP או JPEG אם הדפדפן אינו מקודד WebP.
- הקובץ המקורי אינו נשלח לשרת. הציור מחדש על canvas אינו משמר את מטא־נתוני המקור.
- גם Storage אוכף 50KiB ורשימת MIME מצומצמת. לכל משתמש נתיב יחיד: `USER_ID/avatar`; החלפה דורסת אותו ואינה צוברת גרסאות.
- הפרופיל שומר נתיב קטן עם מזהה גרסה; התמונות מוגשות ישירות מ־Supabase עם מטמון של שעה. הן אינן נשמרות ב־Vercel ואינן משתמשות בשירות Image Optimization שלו.
- 100 משתמשים תופסים לכל היותר כ־4.9MiB לתמונות, ובדרך כלל פחות. עדיין יש לעקוב אחרי תעבורה ומכסות הארגון ב־Supabase.

הדלי ציבורי לקריאת תמונות דרך הכתובת שלהן; שינוי ומחיקה מותרים רק לבעל התמונה. הנתיב היחסי נשמר בשדה הקיים `profiles.avatar_url`, והרכיב המשותף ממיר אותו לכתובת Storage. כתובות HTTPS ישנות ממשיכות להיתמך.

## אימות

בדיקות אוטומטיות בודקות את הרשאות האחסון ועדכון הפרופיל מול PostgreSQL מקומי, את הגדרת מגבלת הקובץ, דחיית קובצי מקור לא מתאימים ורכיב התמונה. בדיקות בדפדפן מאמתות דחיסה בפועל, תצוגה מקדימה, שמירה וביטול. סביבת הבדיקה המקומית אינה תחליף לאימות שירות Storage בפרויקט המרוחק לאחר הרצת המיגרציה.
