# מעקב שימוש למנהל מערכת

יש להריץ ב־Supabase SQL Editor את `supabase/migrations/056_system_resource_usage.sql` ולעדכן את האתר.
הפאנל מופיע בניהול מערכת למנהל־על בלבד. ה־RPC מאמת הרשאה בשרת, אינו משנה נתונים ואינו מחזיר שמות קבצים או מידע אישי.

המדידה כוללת את גודל מסדי הנתונים ב־Postgres ואת סכום גדלי הקבצים מתוך `storage.objects` בכל האחסון של הפרויקט. תמונות פרופיל מוצגות גם בנפרד. קבצים שחסר להם גודל מסומנים כמדידה חלקית ללא יתרה מחושבת.

מכסות ההשוואה הן Free בלבד: 500MB למסד הנתונים ו־1GB לאחסון. היחידות עשרוניות להשוואה שמרנית. המסלול אינו מזוהה אוטומטית. ההתראות מופיעות בפאנל מ־80% ומ־95%, בזמן טעינה/רענון בלבד. אין ניטור רקע, מיילים או חסימה.

זו אינה יתרה מאומתת לחשבון: אחסון בפרויקטים אחרים, תעבורה רגילה ומטמון, שימוש חודשי ב־Auth/Realtime/Functions וכל נתוני Vercel אינם נמדדים. הם מסומנים כלא נבדקו עם קישורים ללוחות הספקים. גם נתוני החיוב/הממוצעים התקופתיים יכולים להיות שונים מהנפח הנוכחי. אין צורך במפתח ניהול בדפדפן או בשירות חיצוני נוסף.

מקורות למכסות ולשיטת המדידה (נבדקו 25.09.2026):
- https://supabase.com/pricing
- https://supabase.com/docs/guides/platform/database-size
- https://supabase.com/docs/guides/platform/manage-your-usage/storage-size
- https://vercel.com/docs/pricing/manage-and-optimize-usage

בדיקות: `npm run check` כולל בדיקת הרשאות SQL למשתמש רגיל/אנונימי/מנהל מערכת, הרצה חוזרת של המיגרציה, ספירת כל האחסון, metadata חסר, התראות וספי מכסות. הבדיקות מקומיות ואינן מאמתות שהמיגרציה הותקנה בענן.
