import {useState} from 'react';import {useMutation,useQuery,useQueryClient} from '@tanstack/react-query';import {Lock,Plus,Shuffle,Trash2,Unlock,Users,UserMinus,UserPlus} from 'lucide-react';import {toast} from 'sonner';import {Badge,Button,Card,FieldHelp,Input,Tooltip} from '../components/ui';import {useGroup,canManage} from '../hooks/useGroup';import {supabase} from '../lib/supabase';import {statusLabel,fullName} from '../lib/utils';import type {Match} from '../types';import {useRealtimeInvalidation} from '../hooks/useRealtime';
const dayNames=['ראשון','שני','שלישי','רביעי','חמישי','שישי','שבת'];
const nextWeekday=(day:number)=>{const d=new Date();d.setHours(12,0,0,0);let add=(day-d.getDay()+7)%7;if(add===0)add=7;d.setDate(d.getDate()+add);return d.toISOString().slice(0,10)};
const perms=[['create_match','פתיחת משחקים'],['close_registration','סגירה ופתיחה של הרשמה'],['generate_teams','ערבוב קבוצות'],['open_ratings','פתיחת דירוגים'],['manage_members','ניהול שחקנים']];
export default function AdminPage(){const {data:g}=useGroup();const qc=useQueryClient();const [tab,setTab]=useState<'matches'|'members'>('matches');const [open,setOpen]=useState(false);const [selectedDay,setSelectedDay]=useState(2);const [f,setF]=useState({title:'משחק TEAMUP',match_date:nextWeekday(2),start_time:'21:00',end_time:'22:30',location:'Gol Time',capacity:'15',team_count:'3',team_size:'5',price_per_player:'0'});const allowed=canManage(g);const {data:matches=[]}=useQuery({queryKey:['admin-matches',g?.group.id],enabled:!!g&&allowed,queryFn:async()=>{const {data,error}=await supabase.from('matches').select('*').eq('group_id',g!.group.id).order('match_date',{ascending:false});if(error)throw error;return data as Match[]}});const {data:members=[]}=useQuery({queryKey:['admin-members',g?.group.id],enabled:!!g&&allowed,queryFn:async()=>{const {data,error}=await supabase.from('group_members').select('*,profiles(*),member_permissions(permission_key)').eq('group_id',g!.group.id).order('joined_at');if(error)throw error;return data as any[]}});useRealtimeInvalidation(`admin-${g?.group.id}`,['matches','match_registrations','group_members','member_permissions'],[['admin-matches',g?.group.id],['admin-members',g?.group.id]],!!g&&allowed);const create=useMutation({mutationFn:async()=>{const {error}=await supabase.from('matches').insert({...f,capacity:+f.capacity,team_count:+f.team_count,team_size:+f.team_size,price_per_player:+f.price_per_player,group_id:g!.group.id,created_by:g!.member.user_id,status:'registration_open'});if(error)throw error},onSuccess:()=>{toast.success('ההרשמה נפתחה');setOpen(false);qc.invalidateQueries()},onError:e=>toast.error(e.message)});const rpc=async(name:string,args:any,success:string)=>{const {error}=await supabase.rpc(name,args);if(error)toast.error(error.message);else{toast.success(success);qc.invalidateQueries()}};const togglePerm=async(m:any,key:string,on:boolean)=>{if(on){const {error}=await supabase.from('member_permissions').upsert({group_member_id:m.id,permission_key:key,granted_by:g!.member.user_id},{onConflict:'group_member_id,permission_key',ignoreDuplicates:true});if(error)return toast.error(error.message)}else{const {error}=await supabase.from('member_permissions').delete().eq('group_member_id',m.id).eq('permission_key',key);if(error)return toast.error(error.message)}toast.success('ההרשאות עודכנו');qc.invalidateQueries({queryKey:['admin-members']})};if(!allowed)return <Card>אין לך הרשאת ניהול.</Card>;return <div className="space-y-5"><div className="page-heading"><div><p>שליטה במשחקים, הרשאות וחברי הקבוצה</p><h1>מרכז ניהול</h1></div><div className="segmented"><button className={tab==='matches'?'active':''} onClick={()=>setTab('matches')}>משחקים</button>{g?.member.role==='admin'&&<button className={tab==='members'?'active':''} onClick={()=>setTab('members')}>שחקנים והרשאות</button>}</div></div>{tab==='matches'?<><Button onClick={()=>setOpen(!open)} title="פתיחת טופס ליצירת משחק והרשמה"><Plus size={18}/>משחק חדש</Button>{open&&<Card className="form-card"><div><h2>פתיחת הרשמה חדשה</h2><p>הגדר יעד של שחקנים וקבוצות. בזמן הערבוב המערכת תתאים את מספר הקבוצות לכמות שנרשמה בפועל.</p></div><div className="form-grid"><div><FieldHelp title="שם המשחק">הכותרת שכל השחקנים יראו.</FieldHelp><Input value={f.title} onChange={e=>setF({...f,title:e.target.value})}/></div><div><FieldHelp title="מיקום">שם המתחם או המגרש.</FieldHelp><Input value={f.location} onChange={e=>setF({...f,location:e.target.value})}/></div><div className="md:col-span-2"><FieldHelp title="באיזה יום משחקים?">בחר יום בשבוע והמערכת תמלא אוטומטית את התאריך הקרוב. אפשר עדיין לשנות ידנית במקרה חריג.</FieldHelp><div className="weekday-picker">{dayNames.map((name,i)=><button type="button" key={name} className={selectedDay===i?'active':''} onClick={()=>{setSelectedDay(i);setF({...f,match_date:nextWeekday(i)})}}>{name}</button>)}</div><div className="date-preview"><span>התאריך שנבחר אוטומטית</span><Input type="date" value={f.match_date} onChange={e=>setF({...f,match_date:e.target.value})}/></div></div><div><FieldHelp title="שעות">שעת התחלה ושעת סיום.</FieldHelp><div className="grid grid-cols-2 gap-2"><Input type="time" value={f.start_time} onChange={e=>setF({...f,start_time:e.target.value})}/><Input type="time" value={f.end_time} onChange={e=>setF({...f,end_time:e.target.value})}/></div></div></div><div><h3 className="field-group-title">תכנון שחקנים וקבוצות</h3><p className="field-group-help">לדוגמה: יעד 15, עד 3 קבוצות, 5 שחקנים בקבוצה. אם נרשמו רק 10, המערכת תיצור 2 קבוצות.</p><div className="form-grid triple"><div><FieldHelp title="יעד נרשמים">כמה מקומות יש בהרשמה.</FieldHelp><Input type="number" value={f.capacity} onChange={e=>setF({...f,capacity:e.target.value})}/></div><div><FieldHelp title="מקסימום קבוצות">הכמות המרבית שתרצה ליצור.</FieldHelp><Input type="number" value={f.team_count} onChange={e=>setF({...f,team_count:e.target.value})}/></div><div><FieldHelp title="גודל קבוצה רצוי">המערכת תשתמש בזה לחישוב אוטומטי.</FieldHelp><Input type="number" value={f.team_size} onChange={e=>setF({...f,team_size:e.target.value})}/></div></div></div><Button disabled={!f.match_date||create.isPending} onClick={()=>create.mutate()}>פתיחת הרשמה</Button></Card>}<div className="admin-match-list">{matches.map(m=><Card key={m.id}><div className="admin-match-row"><div><Badge>{statusLabel(m.status)}</Badge><h2>{m.title}</h2><p>{m.match_date}</p></div><div className="action-row">{m.status==='registration_open'&&<Tooltip label="סגירת ההרשמה כדי לעבור לחלוקת קבוצות"><Button variant="secondary" onClick={()=>rpc('set_match_registration',{p_match_id:m.id,p_open:false},'ההרשמה נסגרה')}><Lock size={17}/>סגירה</Button></Tooltip>}{m.status==='registration_closed'&&<><Tooltip label="פתיחת ההרשמה מחדש לשינויים"><Button variant="secondary" onClick={()=>rpc('set_match_registration',{p_match_id:m.id,p_open:true},'ההרשמה נפתחה מחדש')}><Unlock size={17}/>פתיחה</Button></Tooltip><Tooltip label="חלוקה אוטומטית לפי מספר השחקנים שנרשמו בפועל"><Button variant="secondary" onClick={()=>rpc('generate_balanced_teams',{p_match_id:m.id},'הקבוצות נוצרו')}><Shuffle size={17}/>ערבוב</Button></Tooltip></>}{['teams_published','registration_closed'].includes(m.status)&&!m.ratings_open&&<Button title="פתיחת מסך הדירוג למשתתפי המשחק" variant="secondary" onClick={()=>rpc('open_match_ratings',{p_match_id:m.id},'הדירוג נפתח לשחקני המשחק')}>פתיחת דירוג</Button>}{g?.member.role==='admin'&&<Button title="מחיקת המשחק וכל ההרשמות והקבוצות שלו" variant="danger" onClick={()=>confirm('למחוק את המשחק וכל הנתונים שלו?')&&rpc('delete_match',{p_match_id:m.id},'המשחק נמחק')}><Trash2 size={17}/></Button>}</div></div></Card>)}</div></>:<div className="member-admin-grid">{members.map((m:any)=><Card key={m.id} className={m.status!=='active'?'member-inactive':''}><div className="member-head"><div className="player-avatar">{m.profiles?.first_name?.[0]||'ש'}</div><div><h2>{fullName(m.profiles)}</h2><p>{m.role==='admin'?'מנהל ראשי':m.status==='active'?'חבר פעיל':'הוסר מהקבוצה'}</p></div></div>{m.role!=='admin'&&<>{m.status==='active'&&<div className="permission-grid">{perms.map(([key,label])=>{const on=m.member_permissions?.some((p:any)=>p.permission_key===key);return <label title={`מתן או הסרת הרשאת ${label}`} key={key}><span>{label}</span><input type="checkbox" checked={on} onChange={e=>togglePerm(m,key,e.target.checked)}/></label>})}</div>}<div className="action-row mt-4">{m.status==='active'?<><Button variant="danger" title="הסרת השחקן מהקבוצה. החשבון נשאר וניתן להחזירו" onClick={()=>confirm('להסיר את השחקן מהקבוצה?')&&rpc('remove_group_member',{p_member_id:m.id,p_permanent:false},'השחקן הוסר מהקבוצה')}><UserMinus size={17}/>הסרה</Button><Button variant="ghost" title="מחיקה קבועה של המשתמש והנתונים שלו" onClick={()=>confirm('מחיקה קבועה אינה ניתנת לביטול. להמשיך?')&&rpc('remove_group_member',{p_member_id:m.id,p_permanent:true},'המשתמש נמחק לצמיתות')}><Trash2 size={17}/>מחיקה קבועה</Button></>:<Button title="החזרת השחקן לקבוצה" onClick={()=>rpc('restore_group_member',{p_member_id:m.id},'השחקן הוחזר לקבוצה')}><UserPlus size={17}/>החזרה לקבוצה</Button>}</div></>}</Card>)}</div>}</div>}
