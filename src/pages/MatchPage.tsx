import {useMemo,useState} from 'react';import {useMutation,useQuery,useQueryClient} from '@tanstack/react-query';import {useParams} from 'react-router-dom';import {Check,Clock3,GripVertical,ShieldCheck,Star,UserX,Users} from 'lucide-react';import {toast} from 'sonner';import {Badge,Button,Card,Tooltip} from '../components/ui';import {useAuth} from '../contexts/AuthContext';import {fullName,statusLabel,positionLabel} from '../lib/utils';import {supabase} from '../lib/supabase';import type {Match,Registration} from '../types';import {useRealtimeInvalidation} from '../hooks/useRealtime';import {useGroup,canManage} from '../hooks/useGroup';
const colorNames:any={red:'אדומים',blue:'כחולים',yellow:'צהובים',green:'ירוקים'};
export default function MatchPage(){const {id}=useParams();const {user}=useAuth();const {data:g}=useGroup();const qc=useQueryClient();const [dragged,setDragged]=useState<string|null>(null);const key=['match',id] as const;const q=useQuery({queryKey:key,enabled:!!id,queryFn:async()=>{const {data:m,error}=await supabase.from('matches').select('*').eq('id',id).single();if(error)throw error;const [{data:r,error:re},{data:t,error:te}]=await Promise.all([supabase.from('match_registrations').select('*, profiles(*)').eq('match_id',id).order('registered_at'),supabase.from('teams').select('*,team_players(*,profiles(*))').eq('match_id',id).eq('is_published',true).order('generation_version',{ascending:false}).order('team_number')]);if(re)throw re;if(te)throw te;const latest=(t||[]).length?Math.max(...(t||[]).map((x:any)=>x.generation_version)):0;return {match:m as Match,regs:(r||[]) as Registration[],teams:(t||[]).filter((x:any)=>x.generation_version===latest)}}});useRealtimeInvalidation(`match-${id}`,['matches','match_registrations','teams','team_players','player_ratings'],[key,['v2-home']],!!id);const mine=useMemo(()=>q.data?.regs.find(x=>x.user_id===user?.id),[q.data,user]);const act=useMutation({mutationFn:async(response:'attending'|'unavailable')=>{const {error}=await supabase.rpc('respond_to_match',{p_match_id:id,p_response:response});if(error)throw error},onSuccess:()=>{toast.success('הבחירה נשמרה');qc.invalidateQueries({queryKey:key})},onError:e=>toast.error(e.message)});const move=async(target:string)=>{if(!dragged)return;const {error}=await supabase.rpc('move_team_player',{p_match_id:id,p_user_id:dragged,p_target_team_id:target});if(error)toast.error(error.message);else toast.success('השחקן הועבר והאיזון עודכן');setDragged(null)};if(q.isLoading)return <Card>טוען את פרטי המשחק...</Card>;if(!q.data)return <Card>לא הצלחנו לפתוח את המשחק.</Card>;const {match,regs,teams}=q.data,confirmed=regs.filter(r=>r.registration_status==='confirmed'),wait=regs.filter(r=>r.registration_status==='waitlisted');const teamRatings=teams.map((t:any)=>{const vals=t.team_players.map((p:any)=>Number(p.profiles?.base_rating||3));return vals.length?vals.reduce((a:number,b:number)=>a+b,0)/vals.length:0});const balance=teamRatings.length?Math.max(0,Math.round(100-(Math.max(...teamRatings)-Math.min(...teamRatings))*20)):0;return <div className="space-y-5"><Card className="match-hero"><div className="flex flex-wrap items-start justify-between gap-3"><div><Badge>{statusLabel(match.status)}</Badge><h1>{match.title}</h1><p>{new Date(`${match.match_date}T12:00:00`).toLocaleDateString('he-IL',{weekday:'long',day:'numeric',month:'long'})} · {match.start_time.slice(0,5)} · {match.location||'מיקום יעודכן'}</p></div><div className="capacity-badge"><strong>{confirmed.length}</strong><span>מתוך {match.capacity}</span></div></div>{match.status==='registration_open'&&<div className="match-actions"><Button title="הרשמה למשחק. אם הרשימה מלאה תיכנס לרשימת המתנה" onClick={()=>act.mutate('attending')} disabled={act.isPending}><Check size={18}/>אני מגיע</Button><Button title={mine?.response==='attending'?'ביטול ההרשמה שלי למשחק':'סימון שאינני יכול להגיע השבוע'} variant="secondary" onClick={()=>act.mutate('unavailable')} disabled={act.isPending}><UserX size={18}/>{mine?.response==='attending'?'ביטול הרשמה':'לא יכול השבוע'}</Button></div>}{mine&&<p className="my-status">הסטטוס שלך: {mine.registration_status==='confirmed'?'רשום למשחק':mine.registration_status==='waitlisted'?'ברשימת המתנה':mine.response==='unavailable'?'לא מגיע':'ללא הרשמה'}</p>}</Card>{teams.length>0&&<section className="teams-section"><div className="teams-heading"><div><p>החלוקה פורסמה</p><h2>הקבוצות למשחק</h2></div><Badge><ShieldCheck size={15}/>איזון {balance}%</Badge></div><div className="teams-board">{teams.map((team:any,i:number)=><Card key={team.id} className={`team-card team-${team.color_key||['red','blue','yellow','green'][i]}`} onDragOver={e=>e.preventDefault()} onDrop={()=>move(team.id)}><header><div><span className="team-dot"/><h3>{colorNames[team.color_key]||team.name}</h3></div><strong>{teamRatings[i]?.toFixed(2)||'3.00'}</strong></header><div className="team-player-list">{team.team_players.map((p:any)=><Tooltip key={p.id} label={canManage(g)?'גרור לקבוצה אחרת כדי לשנות את החלוקה':'כרטיס שחקן'}><div draggable={canManage(g)} onDragStart={()=>setDragged(p.user_id)} className="team-player"><GripVertical size={15}/><div className="player-avatar sm">{p.profiles?.first_name?.[0]||'ש'}</div><div><strong>{fullName(p.profiles)}</strong><span>{positionLabel(p.assigned_position||p.profiles?.preferred_position)}</span></div><Star size={14}/></div></Tooltip>)}</div></Card>)}</div>{canManage(g)&&<p className="drag-help">במחשב: ניתן לגרור שחקן בין הקבוצות. במובייל העריכה נשארת דרך יצירת חלוקה מחדש.</p>}</section>}<div className="grid gap-4 lg:grid-cols-2"><Card><div className="section-title"><h2><Users size={19}/>שחקנים שנרשמו</h2><Badge>{confirmed.length}</Badge></div><p className="section-help">הרשימה מתעדכנת מיד אצל כל חברי הקבוצה.</p><div className="players-grid">{confirmed.map((r,i)=><Tooltip key={r.id} label={`נרשם במקום ${i+1}`}><div className="player-row"><b>{i+1}</b><span>{fullName(r.profiles)}</span></div></Tooltip>)}</div></Card><Card><div className="section-title"><h2><Clock3 size={19}/>רשימת המתנה</h2><Badge>{wait.length}</Badge></div>{wait.length?wait.map((r,i)=><div key={r.id} className="wait-row"><span>{fullName(r.profiles)}</span><Badge>#{i+1}</Badge></div>):<p className="empty-inline">אין שחקנים בהמתנה.</p>}</Card></div></div>}
