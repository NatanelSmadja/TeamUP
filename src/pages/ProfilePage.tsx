import {useEffect,useState} from 'react';import {toast} from 'sonner';import {Button,Card,Input,Select,FieldHelp} from '../components/ui';import {useAuth} from '../contexts/AuthContext';import {supabase} from '../lib/supabase';import {Bell,Download,RefreshCw} from 'lucide-react';
const positions=[['goalkeeper','שוער'],['defender','מגן'],['midfielder','קשר'],['winger','כנף'],['striker','חלוץ'],['utility','כללי']];type InstallPromptEvent=Event&{prompt:()=>Promise<void>;userChoice:Promise<{outcome:'accepted'|'dismissed'}>};
export default function ProfilePage(){const {profile,signOut,refreshProfile}=useAuth();const [installPrompt,setInstallPrompt]=useState<InstallPromptEvent|null>(null);const initial=(profile as any)?.preferred_positions||[profile?.preferred_position||'utility'];const [f,setF]=useState({first_name:profile?.first_name||'',last_name:profile?.last_name||'',birth_date:profile?.birth_date||'',preferred_positions:initial as string[],preferred_foot:profile?.preferred_foot||'right'});useEffect(()=>{const h=(e:Event)=>{e.preventDefault();setInstallPrompt(e as InstallPromptEvent)};window.addEventListener('beforeinstallprompt',h);return()=>window.removeEventListener('beforeinstallprompt',h)},[]);const toggle=(p:string)=>setF(x=>({...x,preferred_positions:x.preferred_positions.includes(p)?x.preferred_positions.filter(v=>v!==p):[...x.preferred_positions,p]}));const save=async()=>{if(!f.preferred_positions.length)return toast.error('בחר לפחות עמדה אחת');const {error}=await supabase.from('profiles').update({...f,preferred_position:f.preferred_positions[0]}).eq('id',profile!.id);if(error)return toast.error(error.message);await refreshProfile();toast.success('הפרופיל נשמר')};const install=async()=>{if(installPrompt){await installPrompt.prompt();await installPrompt.userChoice;setInstallPrompt(null)}else toast('באייפון: לחץ שיתוף ואז ״הוסף למסך הבית״')};const update=async()=>{const reg=await navigator.serviceWorker?.getRegistration();await reg?.update();if(reg?.waiting)reg.waiting.postMessage({type:'SKIP_WAITING'});toast.success('בדקנו עדכון')};const notifications=async()=>{if(!('Notification'in window))return toast.error('הדפדפן לא תומך בהתראות');const p=await Notification.requestPermission();toast[p==='granted'?'success':'error'](p==='granted'?'ההתראות הופעלו':'לא ניתן אישור להתראות')};return <div className="space-y-5"><div className="page-heading"><div><p>הפרטים שלך ב־TEAMUP</p><h1>הפרופיל שלי</h1></div></div><Card className="form-card"><div className="form-grid"><div><FieldHelp title="שם פרטי">השם שיופיע בסקרים ובהרשמות.</FieldHelp><Input value={f.first_name} onChange={e=>setF({...f,first_name:e.target.value})}/></div><div><FieldHelp title="שם משפחה">עוזר לזהות שחקנים בעלי שם דומה.</FieldHelp><Input value={f.last_name} onChange={e=>setF({...f,last_name:e.target.value})}/></div></div><FieldHelp title="עמדות מועדפות">אפשר לבחור כמה עמדות. הראשונה תשמש כעמדה הראשית.</FieldHelp><div className="choice-grid">{positions.map(([v,l])=><button title={`בחירת ${l}`} type="button" className={f.preferred_positions.includes(v)?'active':''} onClick={()=>toggle(v)} key={v}>{l}</button>)}</div><FieldHelp title="רגל מועדפת">באיזו רגל נוח לך יותר לשחק?</FieldHelp><Select value={f.preferred_foot} onChange={e=>setF({...f,preferred_foot:e.target.value as any})}><option value="right">ימין</option><option value="left">שמאל</option><option value="both">שתיהן</option></Select><Button className="w-full" onClick={save}>שמירת שינויים</Button></Card><Card><h2 className="font-black">האפליקציה וההתראות</h2><div className="settings-actions"><Button variant="secondary" onClick={install} title="התקנת TEAMUP במסך הבית"><Download size={18}/>הוספה למסך הבית</Button><Button variant="secondary" onClick={update} title="בדיקה אם פורסמה גרסה חדשה"><RefreshCw size={18}/>עדכון האפליקציה</Button><Button variant="secondary" onClick={notifications} title="אישור התראות משחק וסקר במכשיר"><Bell size={18}/>הפעלת התראות</Button></div></Card><Button variant="danger" className="w-full" title="התנתקות מהחשבון במכשיר זה" onClick={signOut}>יציאה מהחשבון</Button></div>}
