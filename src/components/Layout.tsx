import {Activity,BarChart3,CalendarDays,History,Home,LogOut,Plus,Shield,UsersRound,UserRound,Zap} from 'lucide-react';import {NavLink,Outlet,useNavigate} from 'react-router-dom';import {cn} from '../lib/utils';import {useGroup,canManage} from '../hooks/useGroup';import {useAuth} from '../contexts/AuthContext';import AppInstallBanner from './AppInstallBanner';import NotificationCenter from './NotificationCenter';
const base=[['/','בית',Home],['/availability','סקר',CalendarDays],['/squad','קבוצה',UsersRound],['/stats','סטטיסטיקות',BarChart3],['/activity','פעילות',Activity],['/history','היסטוריה',History]] as const;
export default function Layout(){const {data:g}=useGroup();const navigate=useNavigate();const {profile,signOut}=useAuth();const links=canManage(g)?[...base,['/admin','ניהול',Shield] as const]:base;const mobile=links.filter(([to])=>['/','/availability','/squad','/stats','/admin'].includes(to));return <div className="app-shell"><aside className="desktop-sidebar"><div className="brand-mark"><span>⚽</span><div><strong>TEAMUP</strong><small>Play together. Better.</small></div></div><nav>{links.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/' } className={({isActive})=>cn('side-link',isActive&&'active')}><Icon size={20}/><span>{label}</span></NavLink>)}</nav><div className="sidebar-profile"><NavLink to="/profile"><UserRound size={20}/><span>{profile?.first_name||'פרופיל'}</span></NavLink><button onClick={signOut} title="יציאה מהחשבון"><LogOut size={19}/></button></div></aside><div className="content-shell"><header className="desktop-topbar"><div><p>TEAMUP CLUB</p><h2>{g?.group.name||'הקבוצה שלי'}</h2></div><div className="topbar-actions"><NotificationCenter/><NavLink to="/profile" className="profile-chip"><UserRound size={19}/>{profile?.first_name||'פרופיל'}</NavLink></div></header><AppInstallBanner/><main className="page-wrap"><Outlet/></main>{canManage(g)&&<div className="quick-fab"><button title="פעולות מהירות" onClick={()=>navigate('/admin')}><Zap size={20}/><span>פעולות מהירות</span><Plus size={17}/></button></div>}</div><nav className="mobile-nav safe-bottom">{mobile.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/' } className={({isActive})=>cn('mobile-link',isActive&&'active')}><Icon size={20}/><span>{label}</span></NavLink>)}<NotificationCenter/></nav></div>}
